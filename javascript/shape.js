function Shape(x, y){
    this.center = new Point(x, y);
}
Shape.prototype.setCenter = function(point){
    this.center = point;
}
Shape.prototype.draw = function(){};
Shape.prototype.getCenter = function(point){
    return this.center;
}
Shape.prototype.getCenter = function(point){
    return this.center;
}